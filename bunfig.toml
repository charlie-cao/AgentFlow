[install]
# 配置安装选项
cache = true
frozen-lockfile = true
silent = false

[run]
# 配置运行选项
preload = ["./src/server/utils/load-env.ts"]

[test]
# 配置测试选项
coverage = true
preload = ["./src/server/utils/test-setup.ts"]

[build]
# 配置构建选项
target = "bun"
minify = true
sourcemap = true